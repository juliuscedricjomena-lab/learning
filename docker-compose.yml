version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:80"
    volumes:
      - ./sentence:/var/www/html/sentence
      - ./word:/var/www/html/word
      - ./reading:/var/www/html/reading
      - ./listening:/var/www/html/listening
